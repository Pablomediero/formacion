/* Interface: Similar a Type, depende de preferencias para usar una u otra, suele tirar por orientacion a objetos */

interface Personaa { 
    name: string;
    edad: number;
    altura?: number; // Atr?: Atributo Opcional
}